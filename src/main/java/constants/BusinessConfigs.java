package constants;

public enum BusinessConfigs {

    BASE_URL("https://training.by"),
    HOME_PAGE_URL(BASE_URL + "/#/Home"),
    BLOG_PAGE_URL(BASE_URL + "/#/News");

    BusinessConfigs(String bc) {
    }
}
